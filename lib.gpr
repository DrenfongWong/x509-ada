with "common";

library project Lib is

   for Languages use ("Ada", "C");
   for Source_Dirs use ("src/ada", "src/c/pkcs1");
   for Object_Dir use "obj/lib";
   for Library_Dir use "lib";
   for Library_Kind use "static";
   for Library_Name use "x509ada";

   Compiler_Switches := Common.Compiler_Switches & "-gnatwale";

   package Compiler is
      for Default_Switches ("ada") use Compiler_Switches;
   end Compiler;

end Lib;

